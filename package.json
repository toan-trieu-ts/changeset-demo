{
  "name": "changeset-demo",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "version-package": "changeset version",
    "version-package:snapshot": "changeset version --snapshot",
    "release": "changeset publish",
    "release:snapshot": "changeset publish --snapshot --no-git-tag --tag beta"
  },
  "private": true,
  "dependencies": {
    "@changesets/cli": "^2.26.2"
  },
  "devDependencies": {
    "@nx/js": "16.9.1",
    "nx": "16.9.1"
  },
  "workspaces": [
    "packages/*"
  ]
}
